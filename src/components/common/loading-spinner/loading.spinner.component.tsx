import { Loader, LoadingSpinnerContainer } from "./loading.spinner.styled";

export const LoadingSpinner = () => {
  return (
    <LoadingSpinnerContainer>
      <Loader />
    </LoadingSpinnerContainer>
  );
};
